{
  "$schema": "https://oo-ld.org/latest/meta/oold-meta-schema.json",
  "$id": "Sensor.schema.json",
  "@context": {
    "schema": "http://schema.org/",
    "reading": "schema:temperature"
  },
  "title": "Sensor",
  "type": "object",
  "required": ["kind"],
  "properties": {
    "kind": { "type": "string", "const": "Sensor" },
    "reading": { "type": "number" }
  }
}
